const initialState = {
  feedList: null,
  follow: null
};

function reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export const actionCreators = {};

export default reducer;
